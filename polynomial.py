# class for finding 1st derivative from polynomial: (5x^3 + 6x^2 + 7x + 4)` = 5*3 x^2 + 6*2 x + 7 (4 removed)

# class Polynomial:
#
#     def __init__(self, string):
#         self._string = string
#         self._coef = []
#
#
#     def _parse(self):
#         self._parsed = []
#         pos_plus = self._string.find('+')
#         pos_minus = self._string.find('-')
#         if pos_plus < pos_minus:
#
#         for i in self._string:
#             if (i != 'x'):
#                 self._string.find()
#             try:
#                 number = int(i)
#                 self._parsed.append(i)
#             except (TypeError, ValueError) as e:
#                 if i == 'x':
#                     self._p.append(int(self._parsed) if len(self._parsed) > 0 else 1)
#                     self._p

